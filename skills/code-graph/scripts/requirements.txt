# Floors are the first releases that require Python 3.10, so pip refuses to install on 3.9
# instead of resolving old, untested versions. The engine is developed and tested on 3.12.
tree-sitter>=0.24
tree-sitter-language-pack>=1.0
